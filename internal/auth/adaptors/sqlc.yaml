version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "../../../migrations/*.sql"
    gen:
      go:
        package: "adaptors"
        out: "."
        sql_package: "pgx/v5"
